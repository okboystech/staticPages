// import { NextResponse } from 'next/server';
// import { STORAGE } from '@Constants/constants';

// export default function (req) {
// const { cookies, url } = req;
// const isLoggedIn = cookies.get(STORAGE.AUTH);

// if (!isLoggedIn && url.includes('http://localhost:3000/register-brand')) {
//   return NextResponse.redirect('http://localhost:3000/');
// }

// if ((isLoggedIn && url.includes('/otp'))) {
//   return NextResponse.redirect('http://localhost:3000/register-brand');
// };

// };
